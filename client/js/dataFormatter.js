export const scatterFormatter = function(dataset, data){
    if(dataset === "BRK"){
        //add tooltip
        //get scale
        //add color for scale
        //set up legend 
    } else {
        return data;
    }
}